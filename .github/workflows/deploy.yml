name: Deploy Vitaluxe to AWS Lightsail

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production

    env:
      AWS_ROLE_ARN: ${{ secrets.AWS_ROLE_ARN }}
      AWS_REGION: ${{ secrets.AWS_REGION }}
      ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
      LIGHTSAIL_SERVICE_NAME: ${{ secrets.LIGHTSAIL_SERVICE_NAME }}

    steps:
      # 🧩 Step 1: Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 🧾 Step 2: Validate Environment Variables
      - name: Validate Required Secrets (masked presence)
        run: ./.github/scripts/validate-secrets.sh

      # 🔐 Step 3: Configure AWS credentials via OIDC
      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          audience: sts.amazonaws.com
          output-env-credentials: true

      # 🐳 Step 4: Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 🔧 Step 5: Patch vulnerable dependency (temporary hotfix)
      - name: Patch vulnerable dependency
        run: |
          echo "🔧 Updating vulnerable dependencies..."
          npm install cross-spawn@^7.0.5 --save

      # 📦 Step 6: Cache Docker layers
      - name: Cache Docker layers
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      # 🛠️ Step 7: Build Docker image
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          load: true
          tags: vitaluxe-app:latest
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache-new,mode=max

      # 🧪 Step 8: Scan Docker image for vulnerabilities
      - name: Scan Docker image
        continue-on-error: true # 👈 Keeps deployment running even if Trivy finds issues
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'vitaluxe-app:latest'
          format: 'table'
          exit-code: '1'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'

      # 🔑 Step 9: Login & Push to Amazon ECR
      - name: Login & Push to ECR
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
          ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
        run: |
          echo "🔐 Logging into Amazon ECR..."

          # Extract account ID and region from ECR_REPOSITORY
          ACCOUNT_ID=$(echo "$ECR_REPOSITORY" | cut -d'.' -f1)
          REGION=$(echo "$ECR_REPOSITORY" | cut -d'.' -f4)
          echo "Using AWS account: $ACCOUNT_ID"
          echo "Using region: $REGION"

          # Login to ECR using the extracted region
          aws ecr get-login-password --region "$REGION" | docker login --username AWS --password-stdin "$ACCOUNT_ID.dkr.ecr.$REGION.amazonaws.com"

          echo "🏗️ Tagging Docker image..."
          COMMIT_SHA=$(echo "${GITHUB_SHA::7}")
          docker tag vitaluxe-app:latest "$ECR_REPOSITORY:$COMMIT_SHA"
          docker tag vitaluxe-app:latest "$ECR_REPOSITORY:latest"

          echo "📦 Pushing image to ECR..."
          docker push "$ECR_REPOSITORY:$COMMIT_SHA"
          docker push "$ECR_REPOSITORY:latest"

      # 🚀 Step 10: Deploy to AWS Lightsail
      - name: Deploy to Lightsail
        env:
          LIGHTSAIL_SERVICE_NAME: ${{ secrets.LIGHTSAIL_SERVICE_NAME }}
          ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
        run: |
          echo "🚀 Deploying new container version to Lightsail..."
          LIGHTSAIL_SERVICE_NAME=$(echo "$LIGHTSAIL_SERVICE_NAME" | tr -d '\n' | tr -d '\r' | xargs)
          echo "Using Lightsail service: $LIGHTSAIL_SERVICE_NAME"

          aws lightsail create-container-service-deployment \
            --service-name "$LIGHTSAIL_SERVICE_NAME" \
            --containers "{
              \"vitaluxe\": {
                \"image\": \"${ECR_REPOSITORY}:latest\",
                \"ports\": {\"80\": \"HTTP\"}
              }
            }" \
            --public-endpoint "{
              \"containerName\": \"vitaluxe\",
              \"containerPort\": 80
            }"

      # ⏳ Step 11: Wait for Lightsail Deployment & Handle Rollback
      - name: Wait for Lightsail Deployment (with rollback)
        env:
          LIGHTSAIL_SERVICE_NAME: ${{ secrets.LIGHTSAIL_SERVICE_NAME }}
        run: |
          echo "⏳ Waiting for deployment to finish..."
          LIGHTSAIL_SERVICE_NAME=$(echo "$LIGHTSAIL_SERVICE_NAME" | tr -d '\n' | tr -d '\r' | xargs)

          SUCCESS=false
          for i in {1..30}; do
            STATUS=$(aws lightsail get-container-services \
              --query "containerServices[?serviceName=='$LIGHTSAIL_SERVICE_NAME'].state" \
              --output text)

            echo "Current status: $STATUS"
            if [[ "$STATUS" == "RUNNING" ]]; then
              SUCCESS=true
              echo "✅ Deployment successful! Service is running."
              break
            elif [[ "$STATUS" == "DEPLOYING" ]]; then
              echo "Still deploying... waiting 20s"
              sleep 20
            else
              echo "⚠️ Unexpected status: $STATUS"
              break
            fi
          done

          if [ "$SUCCESS" = false ]; then
            echo "❌ Deployment failed or timed out — rolling back to previous version..."
            aws lightsail create-container-service-deployment \
              --service-name "$LIGHTSAIL_SERVICE_NAME" \
              --use-previous-deployment

            echo "🌀 Rollback initiated. Waiting 60s before verifying..."
            sleep 60

            STATUS=$(aws lightsail get-container-services \
              --query "containerServices[?serviceName=='$LIGHTSAIL_SERVICE_NAME'].state" \
              --output text)
            echo "Post-rollback status: $STATUS"
            if [[ "$STATUS" == "RUNNING" ]]; then
              echo "✅ Rollback successful. Service restored to last working version."
            else
              echo "⚠️ Rollback executed but service not yet running. Check Lightsail console."
            fi
          fi

          echo "🔗 Fetching service URL..."
          URL=$(aws lightsail get-container-services \
            --query "containerServices[?serviceName=='$LIGHTSAIL_SERVICE_NAME'].url" \
            --output text)
          echo "🌍 Service URL: $URL"

      # 🧾 Step 12: Final Summary
      - name: Deployment Summary
        if: always()
        run: |
          echo "✅ Vitaluxe deployment process complete."
          echo "If any rollback occurred, review the Lightsail console for details."
