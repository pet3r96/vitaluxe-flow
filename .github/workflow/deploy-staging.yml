name: Deploy Vitaluxe Staging

on:
  workflow_dispatch:  # Manual trigger only for now
  # push:
  #   branches:
  #     - develop  # Enable this later to auto-deploy staging

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - name: Staging Deploy Placeholder
        run: |
          echo "✅ Staging deploy workflow exists — disabled for now."
          echo "🔜 Enable push trigger and add staging container deploy commands later."
